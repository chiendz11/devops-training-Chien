apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgres-localpath.fullname" . }}
  labels:
    {{- include "postgres-localpath.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "postgres-localpath.selectorLabels" . | nindent 4 }}
  ports:
    - name: postgres
      port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
