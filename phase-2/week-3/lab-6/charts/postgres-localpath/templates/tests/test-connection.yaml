apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgres-localpath.fullname" . }}-test-connection"
  labels:
    {{- include "postgres-localpath.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: psql
      image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
      command:
        - sh
        - -c
        - pg_isready -h {{ include "postgres-localpath.fullname" . }} -p {{ .Values.service.port }} -U {{ .Values.postgres.user }}
      envFrom:
        - secretRef:
            name: {{ include "postgres-localpath.secretName" . }}
